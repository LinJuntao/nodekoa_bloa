const config = {
    //启动端口
    port: 3000,
    // 数据库配置
    dataBase: {
        DATABASE: "koabloa",
        USERNAME: "root",
        PASSWORD: "admin",
        PORT: "3306",
        HOST: "localhost"
    }
};
module.exports = config;